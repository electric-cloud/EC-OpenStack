<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN"
    "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">

<html xmlns="http://www.w3.org/1999/xhtml">
<head>
  <meta name="generator" content=
  "HTML Tidy for Windows (vers 25 March 2009), see www.w3.org" />
  <meta content="text/html; charset=us-ascii" http-equiv=
  "content-type" />

  <title>Electric Commander Plugin for OpenStack</title>
  <link rel="stylesheet" href=
  "../../plugins/@PLUGIN_KEY@/pluginhelp.css" type="text/css"
  media="screen" />
</head>

<body>
  <div class="help">
    <h1>OpenStack</h1>

    <p>Plugin Version @PLUGIN_VERSION@</p>
    <hr style=
    "margin-left: -10px; margin-top: 10px; height: 1px; width: 100%; color: #5981BD;"
    noshade="noshade" />

  <p>OpenStack is an Infrastructure as a Service (IaaS) cloud computing project by Rackspace Cloud and NASA. It is free open source software released under the terms of the Apache License.</p>

  <p>The mission of the OpenStack project is to enable any organization to create and offer cloud computing services running on standard hardware. The community's first official release, code-named Austin, was made available just 4 months later with plans to release regular updates of the software every few months.</p>

  <p>OpenStack integrates code from NASA's Nebula platform as well as Rackspace's Cloud Files platform.</p>

  <h2>OpenStack Links</h2>

    <p>More information can be found at <a href=
  "http://www.openstack.org/">OpenStack website</a>, <a href=
  "http://docs.openstack.org/">OpenStack Manuals</a> or <a href=
  "http://docs.openstack.org/api/quick-start/content/">OpenStack API Quick Start</a> .</p>

  <p>Check the <a href=
  "http://api.openstack.org/">OpenStack API Reference</a>.</p>

    <h2>Electric Commander Integration to OpenStack</h2>
    <!-- The integration description should answer the questions:
How deep does our integration go? Why should I care? What general
functionality does the plugin include? What functionality does
the plugin omit?
-->

    <p>The plugin communicates with OpenStack through its
  application programming interface (API) which provides support
  for developers who are building interactive clients of OpenStack using a RESTful application development style.
  EC-OpenStack plugin allows users to build a cloud by launching images and assigning metadata to instances, providing a more generic
  interface for managing resources.</p>

  <p>The plugin interact with OpenStack through the Compute API and Image API using PERL to
  perform the following tasks:</p>

  <ul>
    <li>Create configuration to hold connection information.</li>
    <li>Perform actions on machines.</li>
    <li>Deploy and delete servers.</li>
    <li>Create and delete keypairs.</li>
    <li>Create and delete floating IPs.</li>
    <li>Create Electric Commander resources.</li>
    <li>Create, attach, detach, extend and delete volumes.</li>
    <li>Create new images.</li>
    <li>Create snapshot of a server.</li>
    <li>Hard and soft reboot of a server.</li>
    <li>Deploy, update and delete HEAT orchestration templates.</li>
  </ul>

  <p>This plugin provides two primary actions: Deploy and
  Cleanup, which work as a pair within a job.</p>

    <h2>Integrated Version</h2>

    <p>This plugin was developed and tested against OpenStack IceHouse</p>

    <p>Electric Commander minimum version 3.6</p>

    <h1>Plugin Procedures</h1>

    <p>IMPORTANT: For all parameter descriptions below, required
    parameters are shown in <span class='required'>bold
    italics</span>.</p>

    <div id="CreateConfiguration"><a name='CreateConfiguration' id=
    "CreateConfiguration"></a>

    <h3>Plugin Configuration</h3>
    <!-- If the plugin has separate configurations then include this section,
otherwise omit this whole section. We don't need to have a configuration
section that just says "There is no configuration for this plugin."
-->

    <p>Plugin configurations are sets of parameters that apply across
    some or all of the plugin's procedures. They are intended to
    reduce repetition of common values, create predefined sets of
    parameters for end users, and to securely store credentials
    where needed. Each configuration is given a unique name that is
    entered in designated parameters on procedures that use
    them.<br />
    Plugin configurations are created by going to the Electric
    Commander "Administration" tab, then to the "Plugins" sub-tab.
    On the right side of the line for the specific plugin, there is
    a "Configure" link which will open the Configuration
    page.<br />
    "Service URL" must have http/https followed by your server hostname or ip
    address.</p>
    <!-- For all parameters, the parameter name should be *exactly* the same as it appears in the
    Parameter Panel, and the first sentence(s) of the description should be
    the same as the tooltip (if there is one). After that, the parameter
    description can go into greater detail about the parameter including
    valid value ranges and examples.
    -->

    <table class="grid">
      <thead>
        <tr>
          <th>Parameter</th>

          <th>Description</th>
        </tr>
      </thead>

      <tbody>
        <tr>
        <td class="required">Configuration Name</td>

        <td>Provide a unique name for the configuration, keeping in
        mind that you may need to create additional configurations
        over time. (Required)</td>
      </tr>

      <tr>
        <td class="required">Description</td>

        <td>Provide a simple description for this configuration.
        (Required)</td>
      </tr>

      <tr>
        <td class="required">Service URL</td>

        <td>Provide the host name or IP address of the OpenStack (Required)</td>
      </tr>


          <tr>
              <td class="required">Block Storage URL:</td>

              <td>Provide the host name or IP address of the Block Storage Service for OpenStack (Required)</td>
          </tr>

          <tr>
              <td class="required">Image Service URL</td>

              <td>Provide the host name or IP address of the Image Service for OpenStack (Required)</td>
          </tr>

          <tr>
              <td class="required">Orchestration Service URL</td>

              <td>Provide the host name or IP address of the Orchestration Service for OpenStack (Required)</td>
          </tr>

          <tr>
              <td class="required">Keystone API version</td>

              <td>The Keystone API version. (Required)</td>
          </tr>

          <tr>
              <td class="required">Blockstorage API version</td>

              <td>The OpenStack block storage API version (Required)</td>
          </tr>

          <tr>
              <td class="required">Image API version</td>

              <td>The Image Service API version (Required)</td>
          </tr>



          <tr>
        <td class="required">Configuration Credential
        Parameters</td><!-- Describe *exactly* which user name and password go here.
Is it for a particlar machine? Is it for a website? Is it for a
particular function in the tool? Does it depend on another
parameter (like a machine name) for its meaning?
-->

        <td>OpenStack account username/password. This
        account must have enough privileges to perform API
        functions. (Required)</td>
      </tr>

      <tr>
        <td class="required">Resource</td>

        <td>Indicate the name of the resource (or pool) on which the integration steps can run (default is 'local').
        (Required)</td>
      </tr>

      <tr>
        <td class="required">Workspace</td>

        <td>Indicate the workspace to use for resources dynamically created by this configuration (default is 'default').
        (Required)</td>
      </tr>
      
      <tr>
        <td class="required">Debug Level</td>

        <td>Level for output: 0=errors only, 1=normal headers and
        responses, 2+=debugging information included.
        (Required)</td>
      </tr>
      </tbody>
    </table><img alt="" src=
    "../../plugins/@PLUGIN_KEY@/images/parameters/configuration_panel.png" /></div>

    <div id="Deploy"><a name="Deploy" id="Deploy"></a>

    <h3>Deploy</h3>

    <p>A procedure that calls CreateKey and deploy a new instance(s).<br />
    The plugin will store properties on the Electric Commander server at the
    location provided in "Results Location"/"Results tag"; the
    default location in the job
    (/myJob/Openstack/deployed/"Results tag") is used if
    "Results location" is blank.</p>

    <table class="grid">
      <thead>
        <tr>
          <th>Parameter</th>

          <th>Description</th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td class="required">Configuration</td>

          <td>The name of the config which holds all the connection information. This must reference a valid existing configuration. (Required)</td>
        </tr>

        <tr>
          <td class="required">Tenant ID</td>

          <td>ID of the tenant to use. (Required)</td>
        </tr>

        <tr>
          <td class="required">Key Pair Name</td>

          <td>Existing Key Pair name. (Required)</td>
        </tr>
        
        <tr>
          <td class="required">Number of Servers</td>

          <td>Number of servers to deploy. If more than one, a suffix (_#) is added to server names (default is 1).
          (Required)</td>
        </tr>

        <tr>
          <td class="required">Server name</td>

          <td>Name of the new Server. (Required)</td>
        </tr>

        <tr>
          <td class="required">Image ID</td>

          <td>Id of an existing image in Openstack.
          (Required)</td>
        </tr>

        <tr>
          <td class="required">Flavor ID</td>

          <td>ID for the flavor to use. (Required)</td>
        </tr>

          <tr>
              <td>Availability Zone</td>

              <td>The availability zone in which to launch the server.</td>
          </tr>

          <tr>
              <td>Customization Script</td>

              <td>Configuration information or scripts to execute upon launch of the server. </td>
          </tr>

          <tr>
          <td>Results Location</td>

          <td>The ElectricCommander location to store properties (default is /myJob/OpenStack/deployed).</td>
        </tr>

        <tr>
          <td class="required">Results tag</td>

          <td>The unique tag for this provision to keep it separate from other deployments stored in the same location (default is $[jobStepId]). (Required)</td>
        </tr>
        
        <tr>
          <td>Create Resource?</td>

          <td>Create resources for deployed servers?</td>
        </tr>

        <tr>
          <td>Resource Pool</td>

          <td>The ElectricCommander resource pool name to be associated with the machines in this configuration if resources are created.</td>
        </tr>

        <tr>
          <td>Resource workspace</td>

          <td>The ElectricCommander workspace the resource should use.</td>
        </tr>

        
      </tbody>
    </table><img alt="" src=
    "../../plugins/@PLUGIN_KEY@/images/parameters/deploy_panel.png" /></div>

    <div id="Cleanup"><a name="Cleanup" id="Cleanup"></a>

    <h3>Cleanup</h3>

    <p>This procedure allows users to cleanup a instances created with Deploy.</p>

    <table class="grid">
      <thead>
        <tr>
          <th>Parameter</th>

          <th>Description</th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td class="required">Configuration</td>

          <td>The name of the config which holds all the connection information. This must reference a valid existing configuration. (Required)</td>
        </tr>

        <tr>
          <td class="required">Tenant ID</td>

          <td>ID of the tenant to use. (Required)</td>
        </tr>

        <tr>
          <td class="required">Server ID(s)</td>

          <td>ID of the Server to delete(For more than one server, use a comma-separated list).
          (Required)</td>
        </tr>

        <tr>
          <td>Resource name(s)</td>

          <td>Name of the Resource to delete(For more than one resource, use a comma-separated list).</td>
        </tr>

      </tbody>
    </table><img alt="" src=
    "../../plugins/@PLUGIN_KEY@/images/parameters/cleanup_panel.png" /></div>

    <div id="CreateKeyPair"><a name="CreateKeyPair" id="CreateKeyPair"></a>

    <h3>CreateKeyPair</h3>

    <p>This procedure creates a new key pair with the specified
    name. The public key is stored by OpenStack and the private
    key is returned to you. If a key with the specified name
    already exists, OpenStack returns an error.<br />
    The plugin will store properties on the Electric Commander server at the
    location provided in "Results Location"/"Results tag"; the
    default location in the job
    (/myJob/Openstack/deployed/"Provision tag") is used if
    "Results location" is blank.</p>

    <table class="grid">
      <thead>
        <tr>
          <th>Parameter</th>

          <th>Description</th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td class="required">Configuration</td>

          <td>The name of the config which holds all the connection information. This must reference a valid existing configuration. (Required)</td>
        </tr>

        <tr>
          <td class="required">Tenant ID</td>

          <td>ID of the tenant to use. (Required)</td>
        </tr>

        <tr>
          <td class="required">Key name</td>

          <td>A unique name for the key pair (default is cmdr-$[jobStepId]). (Required)</td>
        </tr>

        <tr>
          <td>Results Location</td>

          <td>The ElectricCommander location to store properties (default is /myJob/OpenStack/deployed).</td>
        </tr>

        <tr>
          <td class="required">Results tag</td>

          <td>The unique tag for this provision to keep it separate from other deployments stored in the same location (default is $[jobStepId]). (Required)</td>
        </tr>
      </tbody>
    </table><img alt="" src=
    "../../plugins/@PLUGIN_KEY@/images/parameters/create_key_panel.png" /></div>

    <div id="DeleteKeyPair"><a name="DeleteKeyPair" id="DeleteKeyPair"></a>

    <h3>DeleteKeyPair</h3>

    <p>This procedure allows the user to delete the specified key
    pair, by removing the public key from OpenStack.You must own the key
    pair.</p>

    <table class="grid">
      <thead>
        <tr>
          <th>Parameter</th>

          <th>Description</th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td class="required">Configuration</td>

          <td>The name of the config which holds all the connection information. This must reference a valid existing configuration. (Required)</td>
        </tr>

        <tr>
          <td class="required">Tenant ID</td>

          <td>ID of the tenant to use. (Required)</td>
        </tr>

        <tr>
          <td class="required">Key name</td>

          <td>Name of the Keypair to delete. (Required)</td>
        </tr>
      </tbody>
    </table><img alt="" src=
    "../../plugins/@PLUGIN_KEY@/images/parameters/delete_key_panel.png" /></div>

    <div id="AllocateIP"><a name="AllocateIP" id="AllocateIP"></a>

    <h3>AllocateIP</h3>

    <p>This procedure allows users to Allocate a public IP address. Run this procedure to acquire a
    Floating IP address for use with your OpenStack account.<br />
    The plugin will store properties on the Electric Commander server at the
    location provided in "Results Location"/"Results tag"; the
    default location in the job
    (/myJob/Openstack/deployed/"Provision tag") is used if
    "Results location" is blank.</p>

    <table class="grid">
      <thead>
        <tr>
          <th>Parameter</th>

          <th>Description</th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td class="required">Configuration</td>

          <td>The name of the config which holds all the connection information. This must reference a valid existing configuration. (Required)</td>
        </tr>

        <tr>
          <td class="required">Tenant ID</td>

          <td>ID of the tenant to use. (Required)</td>
        </tr>

        <tr>
          <td>Results Location</td>

          <td>The ElectricCommander location to store properties (default is /myJob/OpenStack/deployed).</td>
        </tr>

        <tr>
          <td class="required">Results tag</td>

          <td>The unique tag for this provision to keep it separate from other deployments stored in the same location (default is $[jobStepId]). (Required)</td>
        </tr>
      </tbody>
    </table><img alt="" src=
    "../../plugins/@PLUGIN_KEY@/images/parameters/allocate_ip_panel.png" /></div>
     
    <div id="ReleaseIP"><a name="ReleaseIP" id="ReleaseIP"></a>

    <h3>ReleaseIP</h3>

    <p>This procedure allows the user to release a public IP
    address allocated to your account.</p>

    <table class="grid">
      <thead>
        <tr>
          <th>Parameter</th>

          <th>Description</th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td class="required">Configuration</td>

          <td>The name of the config which holds all the connection information. This must reference a valid existing configuration. (Required)</td>
        </tr>

        <tr>
          <td class="required">Tenant ID</td>

          <td>ID of the tenant to use. (Required)</td>
        </tr>

        <tr>
          <td class="required">Floating Address ID</td>

          <td>ID of the Floating IP to release. (Required)</td>
        </tr>
      </tbody>
    </table><img alt="" src=
    "../../plugins/@PLUGIN_KEY@/images/parameters/release_ip_panel.png" /></div>


      <div id="CreateVolume"><a name="CreateVolume" id="CreateVolume"></a>

          <h3>CreateVolume</h3>

          <p>This procedure creates a new volume with the specified
              name. A volume is a detachable block storage device.One can attach a volume
              to one instance at a time.<br />
              The plugin will store properties on the Electric Commander server at the
              location provided in "Results Location"/"Results tag"; the
              default location in the job
              (/myJob/Openstack/deployed/"Provision tag") is used if
              "Results location" is blank.</p>

          <table class="grid">
              <thead>
                  <tr>
                      <th>Parameter</th>

                      <th>Description</th>
                  </tr>
              </thead>

              <tbody>
                  <tr>
                      <td class="required">Configuration</td>

                      <td>The name of the config which holds all the connection information. This must reference a valid existing configuration. (Required)</td>
                  </tr>

                  <tr>
                      <td class="required">Tenant ID</td>

                      <td>ID of the tenant to use. (Required)</td>
                  </tr>

                  <tr>
                      <td class="required">Volume name</td>

                      <td>The display name for the new volume. (Required)</td>
                  </tr>

                  <tr>
                      <td class="required">Size (in GBs)</td>

                      <td>Size in GB for the new volume. (Required)</td>
                  </tr>

                  <tr>
                      <td class="required">Type of volume</td>

                      <td>The type for new volume. (Required)</td>
                  </tr>

                  <tr>
                      <td>Availability zone</td>

                      <td>The availability zone in which create new volume. (Required)</td>
                  </tr>

                  <tr>
                      <td>Results Location</td>

                      <td>The ElectricCommander location to store properties (default is /myJob/OpenStack/deployed).</td>
                  </tr>

                  <tr>
                      <td class="required">Results tag</td>

                      <td>The unique tag for this provision to keep it separate from other deployments stored in the same location (default is $[jobStepId]). (Required)</td>
                  </tr>
              </tbody>
          </table><img alt="" src=
                  "../../plugins/@PLUGIN_KEY@/images/parameters/create_volume.png" /></div>


      <div id="ExtendVolume"><a name="ExtendVolume" id="ExtendVolume"></a>

          <h3>ExtendVolume</h3>

          <p>This procedure extends the size of specified volume.
              The volume must be in "available state" before extending the size.
              <b>Note :</b> Please ensure that the "Extend Volume" operation is supported
              by underlying Openstack services before using this step.
              <br />
              The plugin will store properties on the Electric Commander server at the
              location provided in "Results Location"/"Results tag"; the
              default location in the job
              (/myJob/Openstack/deployed/"Provision tag") is used if
              "Results location" is blank.</p>

          <table class="grid">
              <thead>
                  <tr>
                      <th>Parameter</th>

                      <th>Description</th>
                  </tr>
              </thead>

              <tbody>
                  <tr>
                      <td class="required">Configuration</td>

                      <td>The name of the config which holds all the connection information. This must reference a valid existing configuration. (Required)</td>
                  </tr>

                  <tr>
                      <td class="required">Tenant ID</td>

                      <td>ID of the tenant to use. (Required)</td>
                  </tr>

                  <tr>
                      <td class="required">Volume ID</td>

                      <td>ID of the volume to be extended. (Required)</td>
                  </tr>

                  <tr>
                      <td class="required">New size</td>

                      <td>New size to which the volume to extend to. (Required)</td>
                  </tr>

                  <tr>
                      <td>Results Location</td>

                      <td>The ElectricCommander location to store properties (default is /myJob/OpenStack/deployed).</td>
                  </tr>

                  <tr>
                      <td class="required">Results tag</td>

                      <td>The unique tag for this provision to keep it separate from other deployments stored in the same location (default is $[jobStepId]). (Required)</td>
                  </tr>
              </tbody>
          </table><img alt="" src=
                  "../../plugins/@PLUGIN_KEY@/images/parameters/extend_volume.png" /></div>

      <div id="AttachVolume"><a name="AttachVolume" id="AttachVolume"></a>

          <h3>AttachVolume</h3>

          <p>This procedure attaches volumes created through the volume API to server instances.
              One can attach a volume
              to one instance at a time.<br />
              The plugin will store properties on the Electric Commander server at the
              location provided in "Results Location"/"Results tag"; the
              default location in the job
              (/myJob/Openstack/deployed/"Provision tag") is used if
              "Results location" is blank.</p>

          <table class="grid">
              <thead>
                  <tr>
                      <th>Parameter</th>

                      <th>Description</th>
                  </tr>
              </thead>

              <tbody>
                  <tr>
                      <td class="required">Configuration</td>

                      <td>The name of the config which holds all the connection information. This must reference a valid existing configuration. (Required)</td>
                  </tr>

                  <tr>
                      <td class="required">Tenant ID</td>

                      <td>ID of the tenant to use. (Required)</td>
                  </tr>

                  <tr>
                      <td class="required">Server Id</td>

                      <td>ID of the volume to attach. (Required)</td>
                  </tr>

                  <tr>
                      <td class="required">Volume Id</td>

                      <td>Size in GB for the new volume. (Required)</td>
                  </tr>

                  <tr>
                      <td>Device</td>

                      <td>Name of the device such as, /dev/vdb after attachment.</td>
                  </tr>

                  <tr>
                      <td>Results Location</td>

                      <td>The ElectricCommander location to store properties (default is /myJob/OpenStack/deployed).</td>
                  </tr>

                  <tr>
                      <td class="required">Results tag</td>

                      <td>The unique tag for this provision to keep it separate from other deployments stored in the same location (default is $[jobStepId]). (Required)</td>
                  </tr>
              </tbody>
          </table><img alt="" src=
                  "../../plugins/@PLUGIN_KEY@/images/parameters/attach_volume.png" /></div>

      <div id="DetachVolume"><a name="DetachVolume" id="DetachVolume"></a>

          <h3>DetachVolume</h3>

          <p>This procedure deletes the specified volume attachment from a specified server.
              A volume is a detachable block storage device.<br />
              The plugin will store properties on the Electric Commander server at the
              location provided in "Results Location"/"Results tag"; the
              default location in the job
              (/myJob/Openstack/deployed/"Provision tag") is used if
              "Results location" is blank.</p>

          <table class="grid">
              <thead>
                  <tr>
                      <th>Parameter</th>

                      <th>Description</th>
                  </tr>
              </thead>

              <tbody>
                  <tr>
                      <td class="required">Configuration</td>

                      <td>The name of the config which holds all the connection information. This must reference a valid existing configuration. (Required)</td>
                  </tr>

                  <tr>
                      <td class="required">Tenant ID</td>

                      <td>ID of the tenant to use. (Required)</td>
                  </tr>

                  <tr>
                      <td class="required">Server Id</td>

                      <td>ID of the server from which volume to detach. (Required)</td>
                  </tr>

                  <tr>
                      <td class="required">Attachment Id</td>

                      <td>Volume attachment ID. (Required)</td>
                  </tr>
              </tbody>
          </table><img alt="" src=
                  "../../plugins/@PLUGIN_KEY@/images/parameters/detach_volume.png" /></div>


      <div id="DeleteVolume"><a name="DeleteVolume" id="DeleteVolume"></a>

          <h3>DeleteVolume</h3>

          <p>This procedure deletes the specified volume.
              A volume is a detachable block storage device.<br />
              The plugin will store properties on the Electric Commander server at the
              location provided in "Results Location"/"Results tag"; the
              default location in the job
              (/myJob/Openstack/deployed/"Provision tag") is used if
              "Results location" is blank.</p>

          <table class="grid">
              <thead>
                  <tr>
                      <th>Parameter</th>

                      <th>Description</th>
                  </tr>
              </thead>

              <tbody>
                  <tr>
                      <td class="required">Configuration</td>

                      <td>The name of the config which holds all the connection information. This must reference a valid existing configuration. (Required)</td>
                  </tr>

                  <tr>
                      <td class="required">Tenant ID</td>

                      <td>ID of the tenant to use. (Required)</td>
                  </tr>

                  <tr>
                      <td class="required">Volume ID</td>

                      <td>ID of the volume to be deleted. (Required)</td>
                  </tr>
              </tbody>
          </table><img alt="" src=
                  "../../plugins/@PLUGIN_KEY@/images/parameters/delete_volume.png" /></div>


      <div id="Reboot"><a name="Reboot" id="Reboot"></a>

          <h3>Reboot</h3>

          <p>This procedure reboots the specified server.
              <br />
              The plugin will store properties on the Electric Commander server at the
              location provided in "Results Location"/"Results tag"; the
              default location in the job
              (/myJob/Openstack/deployed/"Provision tag") is used if
              "Results location" is blank.</p>

          <table class="grid">
              <thead>
                  <tr>
                      <th>Parameter</th>

                      <th>Description</th>
                  </tr>
              </thead>

              <tbody>
                  <tr>
                      <td class="required">Configuration</td>

                      <td>The name of the config which holds all the connection information. This must reference a valid existing configuration. (Required)</td>
                  </tr>

                  <tr>
                      <td class="required">Tenant ID</td>

                      <td>ID of the tenant to use. (Required)</td>
                  </tr>

                  <tr>
                      <td class="required">Server ID</td>

                      <td>Id for the server to reboot. (Required)</td>
                  </tr>

                  <tr>
                      <td class="required">Reboot Type</td>

                      <td>Type of the reboot action (hard/soft). (Required)</td>
                  </tr>
              </tbody>
          </table><img alt="" src=
                  "../../plugins/@PLUGIN_KEY@/images/parameters/reboot.png" /></div>




      <div id="CreateImage"><a name="CreateImage" id="CreateImage"></a>

          <h3>CreateImage</h3>

          <p>This procedure creates a virtual machine (VM) image. One can
              launch new instances from these images.<br />
              The plugin will store properties on the Electric Commander server at the
              location provided in "Results Location"/"Results tag"; the
              default location in the job
              (/myJob/Openstack/deployed/"Provision tag") is used if
              "Results location" is blank.</p>

          <table class="grid">
              <thead>
                  <tr>
                      <th>Parameter</th>

                      <th>Description</th>
                  </tr>
              </thead>

              <tbody>
                  <tr>
                      <td class="required">Configuration</td>

                      <td>The name of the config which holds all the connection information. This must reference a valid existing configuration. (Required)</td>
                  </tr>

                  <tr>
                      <td class="required">Tenant ID</td>

                      <td>ID of the tenant to use. (Required)</td>
                  </tr>

                  <tr>
                      <td class="required">Name</td>

                      <td>Display name for the new image. (Required)</td>
                  </tr>

                  <tr>
                      <td class="required">Disk Format</td>

                      <td>The disk format of a VM image. Valid values are AKI,AMI,ARI,Raw,iso, vhd, vdi, qcow2,vmdk. (Required)</td>
                  </tr>

                  <tr>
                      <td class="required">Container format</td>

                      <td>The container format for new image.Valid values are aki, ari, ami, bare, or ovf. (Required)</td>
                  </tr>

                  <tr>
                      <td class="required">Is image location a local file ?</td>

                      <td>Select checkbox if the source image file is residing on local machine. (Required)</td>
                  </tr>

                  <tr>
                      <td class="required">Image path</td>

                      <td>Path of the image on local machine or the valid and direct URL to image location. (Required)</td>
                  </tr>

                  <tr>
                      <td>Size</td>

                      <td>Size of raw image file from which image will be generated.</td>
                  </tr>

                  <tr>
                      <td>Checksum</td>

                      <td>The checksum of raw image file.</td>
                  </tr>

                  <tr>
                      <td>Min-Ram</td>

                      <td>Minimum ram requirement in MBs to run this image on a server.</td>
                  </tr>

                  <tr>
                      <td>Min-Disk</td>

                      <td>Minimum disk space requirement in GBs to run this image on a server.</td>
                  </tr>

                  <tr>
                      <td>Owner</td>

                      <td>The ID of the owner, or tenant, of the image.</td>
                  </tr>

                  <tr>
                      <td>Results Location</td>

                      <td>The ElectricCommander location to store properties (default is /myJob/OpenStack/deployed).</td>
                  </tr>

                  <tr>
                      <td class="required">Results tag</td>

                      <td>The unique tag for this provision to keep it separate from other deployments stored in the same location (default is $[jobStepId]). (Required)</td>
                  </tr>

              </tbody>
          </table><img alt="" src=
                  "../../plugins/@PLUGIN_KEY@/images/parameters/create_image.png" /></div>


      <div id="CreateInstanceSnapshot"><a name="CreateInstanceSnapshot" id="CreateInstanceSnapshot"></a>

          <h3>CreateInstanceSnapshot</h3>

          <p>This procedure creates a snapshot from the instance allowing users
              to launch instances from the snapshot. <br />
              The plugin will store properties on the Electric Commander server at the
              location provided in "Results Location"/"Results tag"; the
              default location in the job
              (/myJob/Openstack/deployed/"Provision tag") is used if
              "Results location" is blank.</p>

          <table class="grid">
              <thead>
                  <tr>
                      <th>Parameter</th>

                      <th>Description</th>
                  </tr>
              </thead>

              <tbody>
                  <tr>
                      <td class="required">Configuration</td>

                      <td>The name of the config which holds all the connection information. This must reference a valid existing configuration. (Required)</td>
                  </tr>

                  <tr>
                      <td class="required">Tenant ID</td>

                      <td>ID of the tenant to use. (Required)</td>
                  </tr>

                  <tr>
                      <td class="required">Instance snapshot name</td>

                      <td>Display name for the new instance snapshot. (Required)</td>
                  </tr>

                  <tr>
                      <td class="required">Server ID</td>

                      <td>ID of the server of which to create snapshot. (Required)</td>
                  </tr>

                  <tr>
                      <td>Metadata</td>

                      <td>Metadata for new instance snapshot in key1,value1,key2,value2 ... format.</td>
                  </tr>
              </tbody>
          </table><img alt="" src=
                  "../../plugins/@PLUGIN_KEY@/images/parameters/create_instance_snapshot.png" /></div>


      <div id="CreateVolumeSnapshot"><a name="CreateVolumeSnapshot" id="CreateVolumeSnapshot"></a>

          <h3>CreateVolumeSnapshot</h3>

          <p>This procedure creates a snapshot, which is a point-in-time
              complete copy of a volume. You can create a volume from the snapshot.  <br />
              The plugin will store properties on the Electric Commander server at the
              location provided in "Results Location"/"Results tag"; the
              default location in the job
              (/myJob/Openstack/deployed/"Provision tag") is used if
              "Results location" is blank.</p>

          <table class="grid">
              <thead>
                  <tr>
                      <th>Parameter</th>

                      <th>Description</th>
                  </tr>
              </thead>

              <tbody>
                  <tr>
                      <td class="required">Configuration</td>

                      <td>The name of the config which holds all the connection information. This must reference a valid existing configuration. (Required)</td>
                  </tr>

                  <tr>
                      <td class="required">Tenant ID</td>

                      <td>ID of the tenant to use. (Required)</td>
                  </tr>

                  <tr>
                      <td class="required">Name of volume snapshot</td>

                      <td>The display name for the new volume snapshot. (Required)</td>
                  </tr>

                  <tr>
                      <td>Description</td>

                      <td>Description of new snapshot.</td>
                  </tr>

                  <tr>
                      <td>Force</td>

                      <td>Indicates whether to create snapshot forcefully of a volume even if it is attached to an instance. (True / False). Default : False</td>
                  </tr>

                  <tr>
                      <td class="required">Volume ID</td>

                      <td>The ID of the volume from which to create a snapshot. (Required)</td>
                  </tr>

                  <tr>
                      <td>Results Location</td>

                      <td>The ElectricCommander location to store properties (default is /myJob/OpenStack/deployed).</td>
                  </tr>

                  <tr>
                      <td class="required">Results tag</td>

                      <td>The unique tag for this provision to keep it separate from other deployments stored in the same location (default is $[jobStepId]). (Required)</td>
                  </tr>

              </tbody>
          </table><img alt="" src=
                  "../../plugins/@PLUGIN_KEY@/images/parameters/create_volume_snapshot.png" /></div>

      <div id="CloudManagerGrow"><a name="CloudManagerGrow" id=
    "CloudManagerGrow"></a>

          <div id="CreateStack"><a name="CreateStack" id="CreateStack"></a>

              <h3>CreateStack</h3>

              <p>Heat is a service to orchestrate multiple composite cloud applications using templates.
                  This procedure creates a new heat stack from  specified heat template.
                   The template can be inputed directly in textbox or the URL of the template can be specified.
                  <br />
                  The plugin will store properties on the Electric Commander server at the
                  location provided in "Results Location"/"Results tag"; the
                  default location in the job
                  (/myJob/Openstack/deployed/"Provision tag") is used if
                  "Results location" is blank.</p>

              <table class="grid">
                  <thead>
                      <tr>
                          <th>Parameter</th>

                          <th>Description</th>
                      </tr>
                  </thead>

                  <tbody>
                      <tr>
                          <td class="required">Configuration</td>

                          <td>The name of the config which holds all the connection information. This must reference a valid existing configuration. (Required)</td>
                      </tr>

                      <tr>
                          <td class="required">Tenant ID</td>

                          <td>ID of the tenant to use. (Required)</td>
                      </tr>

                      <tr>
                          <td class="required">Stack Name</td>

                          <td>Display name for the new stack. (Required)</td>
                      </tr>

                      <tr>
                          <td>Template</td>

                          <td>The stack template to instantiate.</td>
                      </tr>

                      <tr>
                          <td>Template URL</td>

                          <td>A URI to the location containing the stack template to instantiate.Specify URL only if you are not supplying template.</td>
                      </tr>

                      <tr>
                          <td>Results Location</td>

                          <td>The ElectricCommander location to store properties (default is /myJob/OpenStack/deployed).</td>
                      </tr>

                      <tr>
                          <td class="required">Results tag</td>

                          <td>The unique tag for this provision to keep it separate from other deployments stored in the same location (default is $[jobStepId]). (Required)</td>
                      </tr>
                  </tbody>
              </table><img alt="" src=
                      "../../plugins/@PLUGIN_KEY@/images/parameters/create_stack.png" /></div>

          <div id="UpdateStack"><a name="UpdateStack" id="UpdateStack"></a>

              <h3>UpdateStack</h3>

              <p>This procedure updates existing stack with the specified
                  template.<br />
                  The plugin will store properties on the Electric Commander server at the
                  location provided in "Results Location"/"Results tag"; the
                  default location in the job
                  (/myJob/Openstack/deployed/"Provision tag") is used if
                  "Results location" is blank.</p>

              <table class="grid">
                  <thead>
                      <tr>
                          <th>Parameter</th>

                          <th>Description</th>
                      </tr>
                  </thead>

                  <tbody>
                      <tr>
                          <td class="required">Configuration</td>

                          <td>The name of the config which holds all the connection information. This must reference a valid existing configuration. (Required)</td>
                      </tr>

                      <tr>
                          <td class="required">Tenant ID</td>

                          <td>ID of the tenant to use. (Required)</td>
                      </tr>

                      <tr>
                          <td class="required">Stack Name</td>

                          <td>Display name of the stack to update. (Required)</td>
                      </tr>

                      <tr>
                          <td class="required">Stack ID</td>

                          <td>ID of the stack to update. (Required)</td>
                      </tr>

                      <tr>
                          <td>Template</td>

                          <td>The template with which the stack should get updated.</td>
                      </tr>

                      <tr>
                          <td>Template URL</td>

                          <td>A URI to the location containing the updated stack template to instantiate.Specify URL only if you are not supplying template.(One should either supply the template or template URL.)</td>
                      </tr>

                      <tr>
                          <td>Results Location</td>

                          <td>The ElectricCommander location to store properties (default is /myJob/OpenStack/deployed).</td>
                      </tr>

                      <tr>
                          <td class="required">Results tag</td>

                          <td>The unique tag for this provision to keep it separate from other deployments stored in the same location (default is $[jobStepId]). (Required)</td>
                      </tr>
                  </tbody>
              </table><img alt="" src=
                      "../../plugins/@PLUGIN_KEY@/images/parameters/update_stack.png" /></div>

          <div id="DeleteStack"><a name="DeleteStack" id="DeleteStack"></a>

              <h3>DeleteStack</h3>

              <p>This procedure deletes a existing stack with the specified with stack name and stack ID.
                  <br />
                  The plugin will store properties on the Electric Commander server at the
                  location provided in "Results Location"/"Results tag"; the
                  default location in the job
                  (/myJob/Openstack/deployed/"Provision tag") is used if
                  "Results location" is blank.</p>

              <table class="grid">
                  <thead>
                      <tr>
                          <th>Parameter</th>

                          <th>Description</th>
                      </tr>
                  </thead>

                  <tbody>
                      <tr>
                          <td class="required">Configuration</td>

                          <td>The name of the config which holds all the connection information. This must reference a valid existing configuration. (Required)</td>
                      </tr>

                      <tr>
                          <td class="required">Tenant ID</td>

                          <td>ID of the tenant to use. (Required)</td>
                      </tr>

                      <tr>
                          <td class="required">Stack Name</td>

                          <td>Display name of the stack to delete. (Required)</td>
                      </tr>

                      <tr>
                          <td class="required">Stack ID</td>

                          <td>ID of the stack to delete. (Required)</td>
                      </tr>

                      <tr>
                          <td>Results Location</td>

                          <td>The ElectricCommander location to store properties (default is /myJob/OpenStack/deployed).</td>
                      </tr>

                      <tr>
                          <td class="required">Results tag</td>

                          <td>The unique tag for this provision to keep it separate from other deployments stored in the same location (default is $[jobStepId]). (Required)</td>
                      </tr>
                  </tbody>
              </table><img alt="" src=
                      "../../plugins/@PLUGIN_KEY@/images/parameters/delete_stack.png" /></div>


          <h3>CloudManagerGrow</h3>

    <p>This procedure allows the plugin to work with CloudManager
    plugin; it calls Deploy procedure and is intended to be run
    by CloudManager.</p>

    <table class="grid">
      <thead>
        <tr>
          <th>Parameter</th>

          <th>Description</th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td class="required">Configuration</td>

          <td>The name of the config which holds all the connection information. This must reference a valid existing configuration. (Required)</td>
        </tr>

        <tr>
          <td class="required">Tenant ID</td>

          <td>ID of the tenant to use. (Required)</td>
        </tr>

        <tr>
          <td class="required">Number of Servers</td>

          <td>Number of servers to deploy. If more than one, a suffix (_#) is added to server names (default is 1).
          (Required)</td>
        </tr>

        <tr>
          <td class="required">Server name</td>

          <td>Name of the new Server. (Required)</td>
        </tr>

        <tr>
          <td class="required">Image ID</td>

          <td>Id of an existing image in Openstack.
          (Required)</td>
        </tr>

        <tr>
          <td class="required">Flavor ID</td>

          <td>ID for the flavor to use. (Required)</td>
        </tr>

        <tr>
          <td class="required">Results tag</td>

          <td>The unique tag for this provision to keep it separate from other deployments stored in the same location (default is $[jobStepId]). (Required)</td>
        </tr>

        <tr>
          <td class="required">Resource Pool(s)</td>

          <td>The ElectricCommander resource pool names to be associated with the machines in this configuration if resources are created(Space separated list). (Required)</td>
        </tr>
        
        <tr>
          <td>Resource workspace</td>

          <td>The ElectricCommander workspace the resource should use.</td>
        </tr>
      </tbody>
    </table><img alt="" src=
    "../../plugins/@PLUGIN_KEY@/images/parameters/cloudmanager_grow_panel.png" /></div>

    <div id="CloudManagerShrink"><a name="CloudManagerShrink" id=
    "CloudManagerShrink"></a>

    <h3>CloudManagerShrink</h3>

    <p>This procedure allows the plugin to work with CloudManager
    plugin; it calls Cleanup procedure and is intended to be run by
    CloudManager.</p>

    <table class="grid">
      <thead>
        <tr>
          <th>Parameter</th>

          <th>Description</th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td class="required">Configuration</td>

          <td>The name of the config which holds all the connection information. This must reference a valid existing configuration. (Required)</td>
        </tr>

        <tr>
          <td class="required">List of Deployments</td>

          <td>A list of deployments (previously created by
          CloudManagerGrow) to stop. (Required)</td>
        </tr>
      </tbody>
    </table><img alt="" src=
    "../../plugins/@PLUGIN_KEY@/images/parameters/cloudmanager_shrink_panel.png" /></div>

     <div id="CloudManagerSync"><a name="CloudManagerSync" id=
    "CloudManagerSync"></a>

    <h3>CloudManagerSync</h3>

    <p>This procedure allows the plugin to work with CloudManager
    plugin, it verifies that instances it has recorded as "alive"
    are indeed still alive, is intended to be run by
    CloudManager.</p>

    <table class="grid">
      <thead>
        <tr>
          <th>Parameter</th>

          <th>Description</th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td class="required">Configuration</td>

          <td>The name of the config which holds all the connection information. This must reference a valid existing configuration. (Required)</td>
        </tr>

        <tr>
          <td class="required">List of Deployments</td>

          <td>A list of deployments (previously created by
          CloudManagerGrow) to check. (Required)</td>
        </tr>
      </tbody>
    </table><img alt="" src=
    "../../plugins/@PLUGIN_KEY@/images/parameters/cloudmanager_sync_panel.png" /></div>

    <h1>Use Cases and Examples</h1>
    <!-- This section is for examples and use cases, including screenshots of the new Parameter Panel with example values filled in.
Unfortunately, I can't tell you what exactly to put here, but it's OK to put what seems like too much! Include both parameter
values and output where appropriate. Use a separate 'h2' header for each example. Under the h2 header try to stay with the style
outline used above where possible.
-->

    <h2>Use Case 1: Deploy, Consume &amp; Cleanup Job</h2>

    <p>Deploy and cleanup work as a pair within a job.</p>

    <p>The most common use for this plugin is to create a server,
    then create an ElectricCommander resources assigned to it, use the resource(s) to run some
    operations, and the delete the resource and the server. To
    accomplish these tasks you must:</p>

    <ol>
      <li>Create a Plugin Configuration.</li>

      <li>Deploy an OpenStack server.</li>

      <li>Create a Resource(s) for this deployment.</li>

      <li>Use the created resource(s).</li>

      <li>Delete the resource(s).</li>

      <li>Delete the deployed server.</li>
    </ol>

    <h4>Create a Plugin Configuration</h4>

    <p>Plugin configurations are created by going to the Electric
    Commander "Administration" tab, then to the "Plugins" sub-tab.
    On the right side of the line for the EC-OpenStack plugin,
    there is a "Configure" link which will open the Configuration
    page.<br />
    Create a new configuration by specifying the requested
    parameters:</p><img alt="" src=
    "../../plugins/@PLUGIN_KEY@/images/use_cases/Case_1/create_config.png" />

    <p>Once the configuration is created, you can see it listed in
    "OpenStack Configurations", and now you are able to
    deploy a new server.</p>

    <h4>Deploy</h4>

    <p>Create a new Deploy procedure, fill in the requested
    parameters with real values from your OpenStack
    server:</p><img alt="" src=
    "../../plugins/@PLUGIN_KEY@/images/use_cases/Case_1/deploy_parameters.png" />

    <h4>Consume</h4>

    <p>Create a new command step to use the created resource, in
    this example we will pick a resource from the pool and just add
    an ec-perl sleep to use it for 30 seconds</p><img alt="" src=
    "../../plugins/@PLUGIN_KEY@/images/use_cases/Case_1/consume_parameters.png" />

    <h4>Cleanup</h4>

    <p>Now the resource has been used and is ready to be cleaned
    up, create a Cleanup step, fill in the requested
    parameters:</p><img alt="" src=
    "../../plugins/@PLUGIN_KEY@/images/use_cases/Case_1/cleanup_parameters.png" />

    <h4>Results and outputs</h4>

    <p>Once the job finished, you can see properties stored in
    "Results Location".<br />
    <img alt="" src=
    "../../plugins/@PLUGIN_KEY@/images/use_cases/Case_1/job.png" /></p><img alt=""
    src=
    "../../plugins/@PLUGIN_KEY@/images/use_cases/Case_1/results.png" />

    <p>Deploy output:</p><img alt="" src=
    "../../plugins/@PLUGIN_KEY@/images/use_cases/Case_1/deploy_log.png" />

    <p>Cleanup output:</p><img alt="" src=
    "../../plugins/@PLUGIN_KEY@/images/use_cases/Case_1/cleanup_log.png" />


    <h1>Release Notes</h1>
    <h2>@PLUGIN_KEY@ 1.1.2</h2>
    <ul>
      <li>Added properties in 'ec_cloudprovisioning_plugin' property sheet for use in ElectricFlow Deploy</li>
    </ul>
  <h2>@PLUGIN_KEY@ 1.1.1</h2>
  <ul>
    <li>Added keystone v3 authorization</li>
    <li>Decoupled key pair creation/deletion from deploy/cleanup procedures</li>
    <li>Added new procedure: AssociateFloatingIP, which associates floating IP to existing VM</li>
    <li>Fixed create resource option for deploy procedure</li>
  </ul>
	<h2>@PLUGIN_KEY@ 1.0.0</h2>

    <ul>
      <li>Configuration Management.</li>

      <li>Procedures: Deploy, Cleanup, CreateKeyPair, DeleteKeyPair, AllocateIP and ReleaseIP.</li>

      <li>Implemented CloudManager support</li>

      <li>Implemented XML parameter panel</li>

      <li>Added help link to each procedure.</li>
    
      <li>Added help page.</li>

      <li>Added detailed Use Cases.</li>
    </ul>
  </div>
</body>
</html>
